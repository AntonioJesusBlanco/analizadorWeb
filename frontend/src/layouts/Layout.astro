<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro Basics</title>
		<link rel="stylesheet" href="/node_modules/bootstrap/dist/css/bootstrap.min.css" />

	</head>
	<body>
    <nav class="top-nav">
      <div class="nav-content">
        <a href="/" class="nav-logo">MiApp</a>

        <div class="nav-links">
          <a href="/" class="nav-item">Inicio</a>
          <a href="/login" class="nav-item">Login</a>
          <a href="/register" class="nav-item">Registrarse</a>
          <a href="/dashboard" class="nav-item">Dashboard</a>
        </div>
      </div>
    </nav>
		<slot />
	</body>
</html>

<style>
     
  /* ======== GLOBAL ======== */
  body {
    margin: 0;
    font-family: "Inter", Arial, sans-serif;
    background: #0f1115;
    color: #f0f0f0;

    /* Fondo con puntitos SVG */
    background-image: 
      radial-gradient(circle, rgba(255,255,255,0.05) 1px, transparent 1px);
    background-size: 20px 20px;
  }

  .container {
    max-width: 900px;
    margin: 50px auto;
    padding: 0 20px;
  }

  h1, h2 {
    font-weight: 600;
    letter-spacing: -0.5px;
  }

  /* ======== HEADER ======== */
  .header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 35px;
  }

  h1 {
    font-size: 2.3rem;
  }

  /* ======== BUTTONS ======== */
  button {
    cursor: pointer;
    padding: 10px 18px;
    border-radius: 8px;
    border: none;
    background: #ffffff15;
    color: #fff;
    transition: 0.2s;
    backdrop-filter: blur(4px);
  }

  button:hover {
    background: #ffffff25;
  }

  .btn-danger { background: #c0392b99; }
  .btn-danger:hover { background: #c0392bcc; }

  .btn-primary { background: #2980b999; }
  .btn-primary:hover { background: #2980b9cc; }

  .btn-success { background: #27ae6099; }
  .btn-success:hover { background: #27ae60cc; }

  /* ======== CARDS ======== */
  .card {
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.08);
    padding: 28px;
    border-radius: 14px;
    margin-bottom: 35px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.25);
    backdrop-filter: blur(6px);
  }

  .card h2 {
    margin-top: 0;
    margin-bottom: 15px;
  }

  /* ======== INPUT GROUP ======== */
  .input-group {
    display: flex;
    gap: 10px;
  }

  input {
    flex: 1;
    padding: 12px;
    border-radius: 8px;
    border: 1px solid rgba(255,255,255,0.15);
    background: rgba(255,255,255,0.07);
    color: #f0f0f0;
    font-size: 1rem;
    transition: 0.2s;
  }

  input:focus {
    outline: none;
    border-color: #3498db;
    background: rgba(255,255,255,0.12);
  }

  /* ======== METRICS GRID ======== */
  #metrics {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 18px;
  }

  .metric-card {
    background: rgba(255,255,255,0.05);
    border: 1px solid rgba(255,255,255,0.10);
    padding: 20px;
    border-radius: 12px;
    box-shadow: 0 3px 12px rgba(0,0,0,0.25);
    backdrop-filter: blur(5px);
  }
/* ============================= */
/*      LAYOUT & CONTAINERS      */
/* ============================= */

.container {
  max-width: 900px;
  margin: 40px auto;
  padding: 0 20px;
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 35px;
}

.page-title {
  font-size: 2rem;
  margin: 0;
  font-weight: 600;
}

/* ============================= */
/*            SECTIONS           */
/* ============================= */

.section-title {
  margin-bottom: 15px;
  font-size: 1.3rem;
  font-weight: 600;
}

/* ============================= */
/*         INPUT ROWS            */
/* ============================= */

.input-row {
  display: flex;
  gap: 10px;
}

/* ============================= */
/*         METRICS GRID          */
/* ============================= */

.metrics-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(230px, 1fr));
  gap: 18px;
}

.metric-card {
  background: rgba(255,255,255,0.05);
  padding: 20px;
  border-radius: 12px;
  border: 1px solid rgba(255,255,255,0.09);
  box-shadow: 0 3px 10px rgba(0,0,0,0.25);
  backdrop-filter: blur(4px);
  transition: 0.2s ease;
}

.metric-card:hover {
  background: rgba(255,255,255,0.08);
  transform: translateY(-3px);
}

.top-nav {
  position: fixed;
  top: 0;
  width: 100%;
  height: 64px;
  background: rgba(20, 20, 25, 0.7);
  border-bottom: 1px solid rgba(255,255,255,0.08);
  backdrop-filter: blur(12px);
  display: flex;
  align-items: center;
  z-index: 999;
}

.nav-content {
  width: 100%;
  max-width: 1100px;
  margin: 0 auto;
  padding: 0 20px;

  display: flex;
  justify-content: space-between;
  align-items: center;
}

.nav-logo {
  color: #fff;
  font-size: 1.3rem;
  font-weight: 600;
  text-decoration: none;
  letter-spacing: -0.5px;
}

.nav-links {
  display: flex;
  gap: 22px;
}

.nav-item {
  text-decoration: none;
  color: #ccc;
  font-size: 1rem;
  transition: 0.2s;
}

.nav-item:hover {
  color: #fff;
}

/* Empuja el contenido hacia abajo para no quedar debajo del nav */
.page-wrapper {
  padding-top: 90px;
}
</style>
